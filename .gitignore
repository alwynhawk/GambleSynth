# Build output (JUCE is fetched here too — hundreds of MB)
build/
cmake-build-*/

# Render + screenshot artefacts produced by RenderTest
*.wav
ui_*.png

# Skin artwork IS tracked (assets/*.png) - it's part of the build

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
